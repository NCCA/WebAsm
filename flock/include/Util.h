#ifndef UTIL_H__
#define UTIL_H__

/// @brief simple random number generator
extern float randf(float Min=-1.0, float Max=1.0f);
extern float radians(const float _deg );
extern float degrees(const float _deg );

#endif
